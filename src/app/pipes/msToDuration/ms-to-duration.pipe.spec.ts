import { MsToDurationPipe } from './ms-to-duration.pipe';

describe('MsToDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new MsToDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
